@* @page "{subjectId:int}/{quizId:int}"
@model Quizpractice.Pages.Quizs.ResultModel

<h2>Quiz Results</h2>

@if (TempData["Score"] != null)
{
    <div class="alert alert-success">
        You scored @TempData["Score"] out of @TempData["TotalQuestions"]!
    </div>
}

<a href="@Url.Page("/Quizs/Index", new { subjectId = Model.SubjectId, quizId = Model.QuizId })" class="btn btn-secondary">
    Back to Quiz
</a>
 *@